{
  "buildCommand": "pnpm db:generate && ([ \"$VERCEL_ENV\" != \"production\" ] || pnpm --filter @paper-viewer/db exec prisma migrate deploy --schema prisma/schema.prisma) && pnpm --filter @paper-viewer/web build",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/daily-digest",
      "schedule": "0 5 * * 1-5"
    },
    {
      "path": "/api/cron/daily-digest",
      "schedule": "30 5 * * 1-5"
    }
  ],
  "regions": [
    "sin1"
  ]
}
